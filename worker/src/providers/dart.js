export async function getDARTFundamentals(env,ticker){
 if(!env.DART_API_KEY)return {ok:false,mode:"demo",reason:"DART_API_KEY missing"};
 throw new Error("DART provider endpoint/auth mapping is pending official approved API specification.");
}
